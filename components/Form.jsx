import React from 'react'

const Form = ({
      type="Create"
      post={post}
      setPost={setPost}
      submitting={submitting}
      handleSubmit={createPrompt}
}) => {
  return (
    <div>Form</div>
  )
}

export default Form